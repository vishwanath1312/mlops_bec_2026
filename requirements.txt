# ══════════════════════════════════════════════════════════════════════
#  MLOps Workshop — requirements.txt
#  Install with: pip install -r requirements.txt
#  Python version: 3.10 or 3.11
# ══════════════════════════════════════════════════════════════════════

# ── Core ML ───────────────────────────────────────────────────────────
scikit-learn==1.4.2
xgboost==2.0.3
pandas==2.2.2
numpy==1.26.4
imbalanced-learn==0.12.2

# ── Experiment Tracking + Registry (Session 2 & 4) ───────────────────
mlflow==2.13.0

# ── Data Versioning (Session 3) ───────────────────────────────────────
dvc==3.51.2
dvc-s3==3.2.0          # AWS S3 remote
pathspec==0.11.2
# ── Model Serving API (Session 5) ─────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1

# ── Bonus Demo UI (optional — see ui/streamlit_app.py) ────────────────
# Included here so `pip install -r requirements.txt` is all you need to
# run the UI locally. For a leaner Docker image, ui/Dockerfile uses the
# separate ui/requirements.txt instead (streamlit + requests only).
streamlit==1.37.1

# ── Testing (Session 8) ───────────────────────────────────────────────
pytest==8.2.0
httpx==0.27.0          # AsyncClient for FastAPI TestClient

# ── Monitoring (Session 9) ────────────────────────────────────────────
evidently==0.4.33

# ── Cloud & Infrastructure ────────────────────────────────────────────
boto3==1.34.101        # AWS SDK (ECR, S3, EC2 API calls)

# ── Utilities ─────────────────────────────────────────────────────────
joblib==1.4.2          # model serialisation fallback
python-multipart==0.0.9
requests==2.32.2
PyYAML==6.0.1